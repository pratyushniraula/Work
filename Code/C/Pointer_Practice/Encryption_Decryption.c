#include "stdlib.h"
#include "string.h"

int main(int argc, char const *argv[])
{
    /* code */
    //create a file with encryption and decryption
    //file will include the key 
    //another file will include the message you want to encrypt
    //encrypted message will be stored in another file and decrypted will be outputted in the terminal
    //encryption and decryption will be done using XOR operation
    //unordered_map<char, char> key; will store the key

    return 0;
}
